# 🚀 Lovable Agent Onboarding Prompt

> Цей промт для нового Lovable агента в новому акаунті.
> Мета: створити порожній проєкт, підключити GitHub, та якісно ознайомитись з кодовою базою.

---

## КРОК 1: Створення порожнього проєкту

Створи новий React проєкт з наступними характеристиками:

```
Назва: Digital Garden
Технології: React 18 + Vite + TypeScript + Tailwind CSS + shadcn/ui
```

Просто створи мінімальний App.tsx з текстом "Digital Garden — Ready for import".

---

## КРОК 2: Підключення GitHub

Після створення проєкту:
1. Підключи GitHub акаунт
2. Створи новий репозиторій
3. Повідом користувача назву репозиторію

Користувач скопіює код з існуючого репозиторію в новий.

---

## КРОК 3: Ознайомлення з кодовою базою

Після того як користувач підтвердить що код скопійовано, виконай наступне:

### 3.1 Прочитай ключові документи
```
AGENT_HANDOFF.md          — головний handoff документ
src/CLAUDE.md             — інструкції для AI
src/App.tsx               — роутинг
src/components/CLAUDE.md  — структура компонентів
src/hooks/CLAUDE.md       — хуки
src/lib/CLAUDE.md         — бібліотеки
```

### 3.2 Зрозумій архітектуру
- **Frontend**: React + Vite + Tailwind + shadcn/ui
- **Backend Gateway**: Cloudflare Worker (зовнішній)
- **Replit Backend**: FastAPI для Git automation та NotebookLM
- **Контент**: Markdown файли в `src/site/notes/`
- **DRAKON діаграми**: JSON в `src/site/notes/**/diagrams/`

### 3.3 Ключові інтеграції
| Інтеграція | Endpoint | Призначення |
|------------|----------|-------------|
| Owner Auth | `/auth/*` | Авторизація власника |
| Access Zones | `/zones/*` | Гостьовий доступ |
| DRAKON | `/v1/drakon/*` | Git automation для діаграм |
| NotebookLM | `/notebooklm/*` | AI chat |

### 3.4 Перевір ENV
В Lovable Settings має бути:
```
VITE_MCP_GATEWAY_URL=https://garden-mcp.exodus.pp.ua
```

---

## КРОК 4: Валідація

Після ознайомлення:

1. **Відкрий preview** — має завантажитись без помилок
2. **Перевір `/admin/diagnostics`** — gateway status
3. **Перевір циклічну навігацію** — Home → Files → Chat → Graph → Editor → DRAKON → Home
4. **Відкрий `/drakon`** — має показати форму створення

---

## КРОК 5: Готовність до роботи

Після успішної валідації повідом користувача:

```
✅ Проєкт Digital Garden успішно імпортовано та перевірено.

Я ознайомився з:
- Архітектурою (React + Cloudflare Worker + Replit backend)
- Ключовими модулями (Owner Auth, Access Zones, DRAKON, NotebookLM)
- Структурою файлів та компонентів
- Інтеграціями та API endpoints

Готовий продовжити розробку. Що робимо далі?
```

---

## Швидкі команди для навігації

| Що знайти | Де шукати |
|-----------|-----------|
| Роутинг | `src/App.tsx` |
| API calls | `src/lib/api/mcpGatewayClient.ts` |
| Owner auth | `src/hooks/useOwnerAuth.tsx` |
| DRAKON editor | `src/pages/DrakonPage.tsx` |
| Нотатки | `src/site/notes/` |
| Worker код | `infrastructure/cloudflare/worker/` |
| Локалізація | `src/lib/i18n/locales/` |

---

## Важливі правила

1. **НЕ змінюй** `package.json` напряму — тільки через Lovable tools
2. **НЕ ламай** циклічну навігацію в хедері
3. **ЗАВЖДИ** перевіряй Owner auth при додаванні admin-функцій
4. **ОНОВЛЮЙ** всі локалі при зміні текстів (uk/en/fr/de/it)
5. **ТЕСТУЙ** на `/admin/diagnostics` після змін в API

---

## Контакти та ресурси

- **Gateway URL**: `https://garden-mcp.exodus.pp.ua`
- **Replit Backend**: `https://notebooklm-gateway-1.replit.app`
- **GitHub Mirror**: Налаштовується окремо (див. `GITHUB_MIRROR_SETUP.md`)
