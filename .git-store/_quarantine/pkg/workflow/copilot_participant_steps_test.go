//go:build !integration

package workflow

import (
	"testing"
)

// TestBuildCopilotParticipantSteps_EmptyParticipants tests workflow functionality
// SKIPPED: Scripts are now loaded from external files at runtime using require() pattern
func TestBuildCopilotParticipantSteps_EmptyParticipants(t *testing.T) {
	t.Skip("Workflow tests skipped - scripts now use require() pattern to load external files at runtime")
}

// TestBuildCopilotParticipantSteps_IssueAssignee tests workflow functionality
// SKIPPED: Scripts are now loaded from external files at runtime using require() pattern
func TestBuildCopilotParticipantSteps_IssueAssignee(t *testing.T) {
	t.Skip("Workflow tests skipped - scripts now use require() pattern to load external files at runtime")
}

// TestBuildCopilotParticipantSteps_CopilotAssignee tests workflow functionality
// SKIPPED: Scripts are now loaded from external files at runtime using require() pattern
func TestBuildCopilotParticipantSteps_CopilotAssignee(t *testing.T) {
	t.Skip("Workflow tests skipped - scripts now use require() pattern to load external files at runtime")
}

// TestBuildCopilotParticipantSteps_PRReviewer tests workflow functionality
// SKIPPED: Scripts are now loaded from external files at runtime using require() pattern
func TestBuildCopilotParticipantSteps_PRReviewer(t *testing.T) {
	t.Skip("Workflow tests skipped - scripts now use require() pattern to load external files at runtime")
}

// TestBuildCopilotParticipantSteps_CopilotReviewer tests workflow functionality
// SKIPPED: Scripts are now loaded from external files at runtime using require() pattern
func TestBuildCopilotParticipantSteps_CopilotReviewer(t *testing.T) {
	t.Skip("Workflow tests skipped - scripts now use require() pattern to load external files at runtime")
}

// TestBuildCopilotParticipantSteps_CustomToken tests workflow functionality
// SKIPPED: Scripts are now loaded from external files at runtime using require() pattern
func TestBuildCopilotParticipantSteps_CustomToken(t *testing.T) {
	t.Skip("Workflow tests skipped - scripts now use require() pattern to load external files at runtime")
}

// TestBuildCopilotParticipantSteps_MixedParticipants tests workflow functionality
// SKIPPED: Scripts are now loaded from external files at runtime using require() pattern
func TestBuildCopilotParticipantSteps_MixedParticipants(t *testing.T) {
	t.Skip("Workflow tests skipped - scripts now use require() pattern to load external files at runtime")
}
