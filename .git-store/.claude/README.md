# Claude Code - Конфігурація для Garden Bloom

Цей проект налаштований для роботи з Claude Code (autoclaude) для ефективної розробки React + Vite додатку.

## Що налаштовано?

### 📁 Структура `.claude/`

```
.claude/
├── CLAUDE.md              # Головний конфігураційний файл проекту
├── README.md              # Ця інструкція
├── commands/              # Slash команди
│   ├── plan.md           # /plan - планування фіч
│   ├── debug.md          # /debug - допомога з дебагом
│   ├── component.md      # /component - створення компонентів
│   └── review.md         # /review - code review
└── skills/                # Спеціалізовані агенти
    ├── react-debugger.md      # Агент для дебагу
    ├── react-planner.md       # Агент для планування
    └── component-builder.md   # Агент для створення компонентів
```

## Як використовувати?

### 1. Запуск Claude Code

```bash
cd /home/vokov/projects/garden-bloom
claude
```

Claude Code автоматично знайде конфігурацію з `.claude/CLAUDE.md` і буде знати специфіку проекту.

### 2. Slash команди

Використовуй slash команди для швидкого виклику спеціалізованих workflow:

#### `/plan` - Планування нової фічі
```
/plan
```
Допомагає розпланувати нову функціональність:
- Аналізує що вже є в проекті
- Визначає які компоненти потрібні
- Створює детальний план імплементації
- Генерує TODO список

**Приклад:**
```
> /plan
> Я хочу додати форму реєстрації користувача
```

#### `/debug` - Систематичний дебаг
```
/debug
```
Допомагає знайти та виправити проблеми:
- Систематично збирає інформацію про помилку
- Створює і тестує гіпотези
- Перевіряє TypeScript, React, Vite помилки
- Верифікує виправлення

**Приклад:**
```
> /debug
> У мене помилка "Cannot find module '@/components/ui/button'"
```

#### `/component` - Створення компонента
```
/component
```
Допомагає створити новий React компонент:
- З правильною TypeScript типізацією
- З використанням shadcn-ui
- З Tailwind стилями
- З best practices

**Приклад:**
```
> /component
> Мені потрібен компонент для відображення карточки продукту
```

#### `/review` - Code review
```
/review
```
Перевіряє зміни перед комітом:
- TypeScript помилки
- React best practices
- Стилізація
- Валідація форм
- Якість коду

**Приклад:**
```
> /review
> Перевір мої зміни перед комітом
```

### 3. Агенти (Skills)

Агенти - це спеціалізовані AI асистенти для конкретних задач. Вони використовуються автоматично через slash команди, але можна викликати і напряму.

#### React Debugger
Спеціалізований агент для дебагу:
- TypeScript помилки
- React runtime помилки
- Vite build issues
- Стилізація проблеми

**Використання:** автоматично через `/debug`

#### React Planner
Агент для планування архітектури:
- Аналіз вимог
- Дизайн компонентів
- План імплементації
- Ідентифікація ризиків

**Використання:** автоматично через `/plan`

#### Component Builder
Агент для створення компонентів:
- TypeScript інтерфейси
- React компоненти
- shadcn-ui інтеграція
- Tailwind стилізація

**Використання:** автоматично через `/component`

## Робочий процес

### Нова фіча

1. **Планування**
   ```
   /plan
   Опиши що потрібно зробити
   ```

2. **Імплементація**
   - Claude створює TODO список
   - Поетапна розробка
   - Автоматична перевірка після кожного кроку

3. **Перевірка**
   ```
   /review
   ```

4. **Коміт**
   ```
   > Створи коміт з описом змін
   ```

### Виправлення бага

1. **Дебаг**
   ```
   /debug
   Опиши помилку
   ```

2. **Аналіз**
   - Claude збирає інформацію
   - Створює гіпотези
   - Тестує по черзі

3. **Виправлення**
   - Мінімальне виправлення
   - Верифікація (build + lint)

4. **Коміт**

### Створення компонента

1. **Дизайн**
   ```
   /component
   Опиши що потрібно
   ```

2. **Імплементація**
   - TypeScript типи
   - React компонент
   - shadcn-ui компоненти
   - Стилі

3. **Приклад використання**
   - Claude показує як використовувати

4. **Інтеграція**
   - Додавання в потрібне місце

## Технічний стек (що знає Claude)

- **React 18.3** - функціональні компоненти, хуки
- **TypeScript 5.8** - строга типізація
- **Vite 5.4** - build tool
- **shadcn-ui** - UI компоненти на Radix UI
- **Tailwind CSS** - utility-first стилізація
- **React Hook Form + Zod** - форми і валідація
- **TanStack Query** - server state
- **React Router** - клієнтський роутинг

## Корисні команди розробки

Claude знає ці команди і може запускати:

```bash
npm run dev          # Dev server
npm run build        # Production build
npm run lint         # ESLint перевірка
npm run preview      # Preview build

# Додавання shadcn-ui компонента
npx shadcn@latest add [component-name]
```

## Best Practices (що дотримується Claude)

1. **TypeScript** - завжди строга типізація, ніяких `any`
2. **Компоненти** - малі, переиспользуємі, typed
3. **shadcn-ui** - використовуй готові компоненти
4. **Tailwind** - utility classes, `cn()` для умовних
5. **Валідація** - Zod схеми для форм
6. **State** - React Query для server state
7. **Code Style** - ESLint правила проекту

## Приклади використання

### Приклад 1: Додавання форми логіну

```
You: /plan
You: Потрібно додати форму логіну з email і password

Claude: [Створює детальний план]
- Компонент LoginForm
- Zod схема валідації
- React Hook Form setup
- API endpoint через React Query
- Інтеграція в роутинг

You: Так, робимо

Claude: [Поетапно імплементує з TODO списком]
```

### Приклад 2: Дебаг TypeScript помилки

```
You: /debug
You: Помилка: Property 'name' does not exist on type 'User'

Claude: [Систематичний дебаг]
GATHER: Повний контекст помилки
ANALYZE: Перевірка типу User
HYPOTHESES: 3 можливі причини
TEST: Перевірка кожної гіпотези
FIX: Виправлення
VERIFY: Build + lint
```

### Приклад 3: Новий компонент

```
You: /component
You: Карточка товару з зображенням, назвою, ціною і кнопкою "В кошик"

Claude: [Створює компонент]
- TypeScript інтерфейс ProductCardProps
- Використання shadcn-ui Card, Button
- Tailwind стилізація
- Приклад використання
```

## Налаштування під себе

### Додати свою slash команду

Створи файл `.claude/commands/mycommand.md`:

```markdown
---
description: Опис команди
---

# Моя команда

Інструкції для Claude що робити коли викликається /mycommand
```

### Додати свій агент

Створи файл `.claude/skills/my-agent.md`:

```markdown
---
description: Опис агента
skill_type: agent
---

# My Agent

Детальні інструкції для агента...
```

## Troubleshooting

### Claude не бачить конфігурацію

Переконайся що:
- Ти в директорії `/home/vokov/projects/garden-bloom`
- Файл `.claude/CLAUDE.md` існує
- Перезапусти Claude Code

### Slash команда не працює

- Перевір що файл існує в `.claude/commands/`
- Має бути frontmatter з `description`
- Назва файлу = назва команди (без `.md`)

### Агент не викликається

- Перевір що файл в `.claude/skills/`
- Має бути `skill_type: agent` у frontmatter
- Спробуй викликати через відповідну slash команду

## Додаткова інформація

- [Claude Code Docs](https://docs.claude.com/claude-code)
- [shadcn-ui Docs](https://ui.shadcn.com/)
- [React Docs](https://react.dev/)
- [Vite Docs](https://vitejs.dev/)

---

**Готовий до розробки? Запускай `claude` і починай працювати!** 🚀
